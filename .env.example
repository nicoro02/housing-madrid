# Puertos (cambia si chocan con lo que ya tengas corriendo)
MONGO_PORT=27017
POSTGRES_PORT=5432
MLFLOW_PORT=5001

# Mongo
MONGO_USER=admin
MONGO_PASSWORD=admin
MONGO_URI=mongodb://admin:admin@localhost:27017

# Postgres
POSTGRES_USER=housing
POSTGRES_PASSWORD=housing
POSTGRES_DB=housing
POSTGRES_URI=postgresql://housing:housing@localhost:5432/housing

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5001

# Scraper
SCRAPE_MIN_DELAY_SEC=2.5
SCRAPE_MAX_DELAY_SEC=5.5
SCRAPE_USER_AGENT=Mozilla/5.0 (educational-portfolio; contact nicoro02@ucm.es)

# GCP (rellenar al desplegar)
GCP_PROJECT=
GCP_REGION=europe-southwest1
